<h1>Список пользователей</h1>

<table>
  <tr>
    <th>Имя</th>
    <th>Э-почта:</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Вы уверенны?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новый пользователь', new_user_path %>
