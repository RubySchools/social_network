<!DOCTYPE html>
<html>

	<%= render 'layouts/style_and_meta' %>

  <body>
 <div class="container">

	<%= render 'layouts/header' %>
	  
	<div>
		<p id="notice"><%= notice %></p>
		<p id="alert"><%= alert %></p>
	</div>
  
	<section class="round">
		<%= yield %>
	</section>
</div>
</body>
</html>
